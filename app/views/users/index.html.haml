-# User#index
-# map and filets fluid row
#filled.row-fluid
 #baseContainerFluid.span9.side-lined
  #map

   Listing users

   %table
    %tr
     %th Name
     %th Password digest
     %th
     %th
     %th

   - @users.each do |user|
    %tr
     %td= user.name
     %td= user.password_digest
     %td= link_to 'Show', user
     %td= link_to 'Edit', edit_user_path(user)
     %td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

   %br

   = link_to 'New User', new_user_path

 #filtersContainerFluid.span3.right-lined
  Бла-бла.